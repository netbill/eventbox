version: "2"

sql:
  - schema: pg/schemas
    queries: pg/queries
    engine: postgresql
    gen:
      go:
        package: sqlc
        out: pg/sqlc
        sql_package: "pgx/v5"

#        overrides:
#          - db_type: "uuid"
#            go_type:
#              import: "github.com/google/uuid"
#              type: "UUID"
#          - db_type: "timestamptz"
#            go_type:
#              import: "time"
#              type: "Time"

